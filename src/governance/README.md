# Governance

This folder holds governance metadata and lightweight validation functions meant to be used by the UI and CI to enforce naming and metadata standards across fact sheets.

Guidelines
- Place policy rules in `rules.ts`.
- Call validators before saving or onboarding templates.
